node_modules/
dist/
.DS_Store
*.log
# the service-account key is a REAL credential (unlike the old public web
# config) — it belongs in host env vars only, never in the repo. See docs/FIREBASE.md
*serviceAccount*.json
firebase-service-account.json
.env
.env.*
.vercel/
# derived by `npm run data` — never edit, never commit
public/data/index.json
public/data/network-lite.geojson
public/data/network-detail.geojson
public/data/shapes/
public/data/orgs.json
public/data/org/
# raw OpenStreetMap downloads used to generate the corpus (large, re-fetchable)
.cache/
